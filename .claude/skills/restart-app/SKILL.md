---
name: Restart app
description: Restart the local app server using the project-approved restart script. Use whenever the app/server/dev server needs to be started, restarted, relaunched, recovered after a port conflict, or verified locally.
---

# Restart app

When the app needs to be started or restarted, always use the project restart script:

```bash
./restart-server.sh
```
